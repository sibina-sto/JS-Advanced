function solve(first, second, third){
    const result = first.length + second.length + third.length;
    console.log(result);
    console.log(Math.floor( result / 3 ));
}

//solve('chocolate', 'ice cream', 'cake');
